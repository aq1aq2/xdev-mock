Note:
- How these properties file loaded into program ?
Based declaration template dir and template value in struts.xml
- How to write ftl file ?
Using freemarker script (.xhtml)

This example use extending theme technique:
http://struts.apache.org/2.0.6/docs/extending-themes.html

How to wrap a theme:
<#include "/${parameters.templateDir}/${parameters.theme}/controlheader.ftl" /> --> custom
<#include "/${parameters.templateDir}/simple/text.ftl" /> --> basic
<#include "/${parameters.templateDir}/xhtml/controlfooter.ftl" /> --> basic

Freemarker -> make template in html with some directive dynamically.
- Freemarker syntax ? --> Reading

You should use template dir value = "template" to use both custom template and basic template

Data model (Java object) + Template (ftl) = Output

Note for programmers: 
A non-existent variable and a variable with null value is the same for FreeMarker, so the "missing" term used here covers both cases.

?? and ! in dealing with missing variables.

When framework call freemarker ?
FTL use for insteading jsp or mix with jsp file

FreeMarker is not a Web application framework. 
It is suitable as a component in a Web application framework, but the FreeMarker engine itself knows nothing about HTTP or servlets. 
It simply generates text.

$ -> replace by actual output // interpolation
# -> ftl tag
<#-- comment -->

http://struts.apache.org/2.0.14/docs/using-freemarker-templates.html#UsingFreemarkerTemplates-FreeMarkeralternativesyntax
http://struts.apache.org/2.0.6/docs/themes-and-templates.html
http://jqueryui.com/demos/dialog/
